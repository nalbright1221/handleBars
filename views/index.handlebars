<h1>Drinks!</h1>

<h2>Drinks that need drank!</h2>

<ul>
  {{#each drinks}}
    {{#unless chugIt}}
    <li style="list-style: none">
        {{this.drink_name}}
          <button class="chugItButton" drinkID={{this.id}}>Chug It!</button>
          <!---data - something data attributes <--->
    </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Drinks that have been drank!</h2>

<ul>
  {{#each drinks}}
    {{#if chugIt}}
      <li>
          {{this.drink_name}}
          <!---data - something data attributes to make a drink againa and drink it  <--->
      </li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Drink</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Drink Name:</label>
    <input type="text" id="ca" name="name">
  </div>
<!--retreieve value in text box<!---> 
  <button  id="createNewDrink" >Add Drink</button>
</form>
